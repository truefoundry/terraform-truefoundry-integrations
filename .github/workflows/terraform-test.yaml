name: OpenTofu Test
on: [pull_request]

jobs:
  test:
    # TODO: repin to a tagged/main ref once the reusable workflow lands on main
    # of github-workflows-public (currently only on feat/uniform-aws-tagging).
    uses: truefoundry/github-workflows-public/.github/workflows/terraform-test.yml@feat/uniform-aws-tagging
